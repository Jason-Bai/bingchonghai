import React, { Component } from 'react'
import { Card, Form } from 'antd'
import { FormModal, SearchBar } from '../index'

const FormItem = Form.FormItem

export default class FilterNav extends Component {

  render() {
    return (
      <div className="filterNav">
        <Card>
          filternav
        </Card>
      </div>
    )
  }
}
